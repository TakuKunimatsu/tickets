        <div class="col">
          <div class="card shadow-sm">  
            <%= image_tag performance.image %>
            <div class="card-body">
              <p class="card-text">団体名：<%= performance.admin_user.company_name %></p>
              <p class="card-text">公演名：<%= performance.name %></p>
              <p class="card-text">劇場：<%= performance.theater %></p>
              <p class="card-text">日程：<%= performance.start_day %>〜<%= performance.last_day %></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <button type="button" class="btn btn-sm btn-outline-secondary" ><%= link_to 'View', performance_path(performance.id), class: "btn btn-sm" %></button>
                  <% if admin_user_signed_in? %>
                    <% if current_admin_user.id == performance.admin_user_id %>
                    <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                    <% end %>
                  <% end %>
                </div>
                <small class="text-muted">9 mins</small>
              </div>
            </div>
          </div>
        </div>
